
## Hardware & Infrastructure
- **VM RAM Upgrade:** Simon increased the VM RAM to **24GB** (Proxmox host: Mac Mini 32GB) to support larger local models.
- **Local Model Success:** Successfully pulled and validated **Gemma 3 (12B)**. It demonstrated faster latency than Gemini 3 Flash for local tool-heavy tasks due to zero network overhead.
- **Config Update:** Added `ollama/gemma3:12b` to the allowlist in `openclaw.json` with a 128K context window.

## AI Strategy (Tiered Brain)
- **Tiered Brain Strategy:** Implemented a new orchestration plan in `SOUL.md`:
    - **Tier 1 (Default):** Gemma 3 (12B) for 90% of routine chat and scheduling.
    - **Tier 2 (Manual):** Use tags (`[pro]`, `[flash]`, `[3.1]`) for manual Gemini escalation.
    - **Tier 3 (Auto-Escalation):** Proactive escalation for complex code debugging or history >50k tokens.

## User Preferences & Logic
- **Default Location:** Updated `USER.md` to establish **Chesil Street, Winchester** as the default location. Logic for travel and meeting planning will assume Simon is home unless stated otherwise or overridden by Telegram location updates.
- **Email Summary Rescheduling:** Updated `email-summary` cron to run at **12:00 and 17:00** only, as the 08:00 slot is covered by the Morning Briefing note.

## Technical Resolutions
- **Telegram Delivery Fix:** Moved `email-summary` and briefings to use direct `message` tool calls with explicit chat IDs to resolve recurring "announce" delivery failures.
